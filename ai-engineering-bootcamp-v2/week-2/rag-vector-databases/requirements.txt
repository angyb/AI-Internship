# Production deps for the Week 2 RAG FastAPI service (Render).
# For the notebook locally: pip install -r requirements-dev.txt
#
# CPU-only PyTorch — must appear before sentence-transformers so pip does not
# pull the default CUDA/nvidia stack (~2GB+) on Linux (Render).

--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.5.0

fastapi[standard]
openai
python-dotenv
pinecone
langchain-core
langchain-openai
langchain-text-splitters
pymupdf
pypdf
ragas>=0.4.3
sentence-transformers>=3.4.0
rank-bm25
sqlalchemy>=2.0
psycopg[binary]>=3.1
