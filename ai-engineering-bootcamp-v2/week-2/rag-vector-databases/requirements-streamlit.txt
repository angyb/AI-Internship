# Render Streamlit UI — calls POST /agent on the RAG API service (no google-adk here).
# Pin past 1.56 so filter_mode is available when the platform installs a fresh env.
streamlit>=1.56.0,<2
httpx>=0.27.0
python-dotenv>=1.0.0
